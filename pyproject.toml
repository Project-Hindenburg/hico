[project]
name = "hico"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
  "torch",
  "transformers",
  "numpy",
  "pandas",
  "matplotlib",
  "ipykernel",
  "seaborn>=0.13.2",
]


